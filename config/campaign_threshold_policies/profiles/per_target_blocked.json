{
  "schema_version": 1,
  "name": "per_target_blocked",
  "allow_per_target_thresholds": true,
  "require_matching_thresholds": true,
  "notes": "Allow per-target semantics, but keep threshold-schema matching as a hard requirement. This remains blocked by mismatch."
}
